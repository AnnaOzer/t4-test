принято всё, что не является моделью, вью или контроллером называть компонентом
новая папка protected/Components
в ней новый класс Test в пространстве имён App/Components
класс унаследован от Std

---

пусть
хотим перехватывать обращение к свойству Foo
при попытке записать что-то в свойство foo будет вызван метод:
в классе Test пишем функцию
protected function setFoo($value)
название функции
состоит из слова set
и названия свойства с большой буквы

один метод - одно свойство

глобальный метод на все свойства - есть,
надо переопределить родительский сеттер
часто полезно перехватить установку только одного магического значения

---
пусть мы хотим хранить в свойствоах то что туда пришло
но что-то делать при извлечении этих свойств
создаём метод
getFoo
который будет всегда вызываться при попытке доступа на чтение к свойству foo

через форич не сработало
часть функций фреймворка еще не написаны
надо добавить в код форка фреймворка foreach

